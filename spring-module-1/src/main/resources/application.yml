spring:
  datasource:
    url: ${DATABASE_URL}
    password: ${DATABASE_USERNAME}
    username: ${DATABASE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
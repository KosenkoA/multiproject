server:
  port: 8080
spring:
  datasource:
    password: postgres
    url: jdbc:postgresql://java_db:5432/postgres
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
